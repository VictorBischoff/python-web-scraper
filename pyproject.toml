[tool.poetry]
name = "python-web-scraper"
version = "0.1.0"
description = "A Python web scraping project for automated data extraction"

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"